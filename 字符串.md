###字符串
```
let str = "\\\""
print(str)

//unicode：swift优点：一个字符可以表示复杂的信息,因此字符串计算起来很方便
let coolGuy = "\u{1F60E}"
print(coolGuy)

//swift计算字符串长度
var coolLetters = "liubinpeng"
coolLetters.characters.count

var lesson = "刘斌鹏"
lesson.characters.count
lesson == coolLetters //字符串比较

```