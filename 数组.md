###数组
```
import UIKit
/*
 * 集合：数组、字典、set
 */
var array = Array<Int>(repeating:8,count:9)

var array2 = [1,2,3,34,21,345,1122]
array2.max()
array2.min()

array2.isEmpty
array2.first
array2.last
array2[array2.count-2]
//数组排序
array2.sort { (a, b) -> Bool in
 return a>b
}
array2.sort()

array2.sorted()
array2.sort { (a, b) -> Bool in
 return a<b
}

//数组遍历
for index in 0..<array2.count{
 array2[index]
}

for num in array2{
 num
}

for (index,value) in array2.enumerated(){
 print("\(index+1):\(value)")
}

//数组增删改查

//增
array2.append(2)
array2.insert(222, at: 2)
array2 += [344]
array2 = array2 + [12345]
//删
array2.removeLast()
array2
array2.removeFirst()
array2.remove(at: 2)
array2

array2.removeSubrange(0...3)
array2.removeFirst(1)
/*
 ** remove方法使用如下 remove(at: 3)
 removeFirst 和 removeLast也可以传入一个参数，用于从头或者从尾开始删除直到指定索引处为止
 removeSubrange是用于删除指定索引范围内的值 removeSubrange(0..<4);
 */
//改
array2[0] = 1
array2[1] = 2
array2
array2[0...2] = [0,1,2]

//查
array2.contains(12345)
array2.index(of: 12345)

/*
 * 二维数组
 */
var board:Array<Array<Int>> = [[1024,14,2,0],
 [233,23,45,1],
 [34,0,0,0],
 [245,2,904,2]]
board[0]
board[0][0]
board.count

board[0].append(5)
board
board[0].count

board.append([1,1,1])
board

board += [ [2,2,2,2] ]
board
```